site:
  url: 'http://localhost/leafcutter/web/'

templates:
  site:
    name: Demo site

cache:
  driver: ephemeral
  directory: '${base_dir}/cache/'
  driver_filesystem_config:
    encoder: Native

assets:
  output:
    directory: '${base_dir}/assets/'
    url: '@/assets/'

plugins:
  system_staticpages:
    enabled: false
    ttl: 30

theme:
  css:
    bundle: false
    max_inlined: 0
    minify: false
  js:
    bundle: false
    minify: false
    inline_loader: false
